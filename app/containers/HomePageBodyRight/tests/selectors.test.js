// import { fromJS } from 'immutable';
// import { makeSelectHomePageBodyRightDomain } from '../selectors';

// const selector = makeSelectHomePageBodyRightDomain();

describe('makeSelectHomePageBodyRightDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
