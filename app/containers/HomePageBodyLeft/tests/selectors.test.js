// import { fromJS } from 'immutable';
// import { makeSelectHomePageBodyLeftDomain } from '../selectors';

// const selector = makeSelectHomePageBodyLeftDomain();

describe('makeSelectHomePageBodyLeftDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
