// import { fromJS } from 'immutable';
// import { makeSelectHomePageBodyDomain } from '../selectors';

// const selector = makeSelectHomePageBodyDomain();

describe('makeSelectHomePageBodyDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
